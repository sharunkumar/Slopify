name: 🐟 🐠 🐡
on: [push, workflow_dispatch]

jobs:
  print-wahoo:
    runs-on: macos-latest
    steps:
      - name: Print wahoo fish
        run: |
          for i in {1..1000}; do
            echo "wahoo fish"
            echo "-------------------------"
            sleep 0.125
          done